/**
 * @overview generated by ghoti-cli
 * @fileoverview Webpack Config Renderer Devlopment
 */

const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');
const webpack = require('webpack');

const BUILD_DIR = path.resolve(__dirname, '..', 'dist', 'editParser');
const APP_DIR = path.resolve(__dirname, '..', 'src', 'editParser');
const PUBLIC_DIR = path.resolve(__dirname, '..', 'public', 'template.html');
const PAGECLIP_DIR = path.resolve(__dirname, '..', 'public', 'pageclip.html');
const RENDERERTSCONFIG_DIR = path.resolve(__dirname, 'tsconfigParser.json');

let config = {
    devtool: 'cheap-module-eval-source-map',
    target: "electron-renderer",
    entry: {
        index: [
            'react-hot-loader/patch',
            'webpack-dev-server/client',
            'webpack/hot/only-dev-server',
            APP_DIR + "/index.tsx",
        ],
        // report: [
        //     'react-hot-loader/patch',
        //     'webpack-dev-server/client',
        //     'webpack/hot/only-dev-server',
        //     APP_DIR + "/bugReport/index.tsx",
        // ],
    },
    output: {
        filename: "[name].bundle.js",
        path: BUILD_DIR,
        publicPath: '/'
    },
    resolve: {
        extensions: [".ts", ".tsx", ".js", ".json", ".css", ".sass"]
    },
    module: {
        rules: [{
                test: /\.tsx?$/,
                use: [{
                    loader: 'awesome-typescript-loader',
                    options: {
                        configFileName: RENDERERTSCONFIG_DIR
                    }
                }]
            },
            {
                test: /\.sass$/,
                use: [
                    'style-loader',
                    'css-loader',
                    'sass-loader'
                ]
            },
            {
                enforce: "pre",
                test: /\.js$/,
                loader: "source-map-loader",
            }
        ]
    },
    plugins: [
        new HtmlWebpackPlugin({
            chunks: ['index'],
            title: 'p6',
            template: PUBLIC_DIR,
            filename: 'index.html',
        }),
        // new HtmlWebpackPlugin({
        //     chunks: ['report'],
        //     title: 'p6',
        //     template: PAGECLIP_DIR,
        //     filename: 'report.html',
        // }),
        new webpack.DefinePlugin({
            'process.env.NODE_ENV': JSON.stringify(process.env.NODE_ENV || 'development')
        }),
        new webpack.LoaderOptionsPlugin({
            debug: true
        }),
        new webpack.HotModuleReplacementPlugin(),
        new webpack.NamedModulesPlugin()
    ],
    devServer: {
        hot: true,
        contentBase: path.resolve(__dirname, 'dist', 'editParser'),
        publicPath: '/',
        port: 8080,
        inline: true,
        historyApiFallback: true
    }
};

module.exports = config;